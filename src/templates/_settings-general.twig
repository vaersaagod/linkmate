{% import '_includes/forms' as forms %}

{{ forms.selectField({
  label: 'Default link type'|t('linkmate'),
  instructions: 'Select the preselected link type.'|t('linkmate'),
  id: 'defaultLinkName',
  name: 'defaultLinkName',
  options: linkNames,
  value: settings.defaultLinkName,
}) }}

{{ forms.textField({
  label: 'Default link text'|t('linkmate'),
  instructions: 'Set the default value of the link text.'|t('linkmate'),
  name: 'defaultText',
  value: settings.defaultText
}) }}

{{ forms.checkboxField({
  label: 'Allow custom link text'|t('linkmate'),
  name: 'allowCustomText',
  checked: settings.allowCustomText
}) }}

<hr/>

{{ forms.checkboxField({
  label: 'Allow links to open in new window'|t('linkmate'),
  name: 'allowTarget',
  checked: settings.allowTarget
}) }}

{{ forms.checkboxField({
  label: 'Set link relation to "noopener noreferrer" when opening links in a new window'|t('linkmate'),
  name: 'autoNoReferrer',
  checked: settings.autoNoReferrer
}) }}

<hr/>

{{ forms.checkboxField({
  label: 'Enable aria label support'|t('linkmate'),
  name: 'enableAriaLabel',
  checked: settings.enableAriaLabel
}) }}

{{ forms.checkboxField({
  label: 'Enable title support'|t('linkmate'),
  name: 'enableTitle',
  checked: settings.enableTitle
}) }}
